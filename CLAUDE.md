# Go Learn 协作规范

> 本文件与 `AGENTS.md` 是同一套规范的两个入口，内容必须保持一致；修改其中一个时必须同步另一个。

## 新增章节清单

实现一个新章节时，必须同步完成以下内容：

1. 在 `internal/chapter/` 下新增章节包，命名为 `goNN_主题/`（例如 `go05_functions/`），包内提供 `Demo()` 函数。
2. 在 `cmd/go-learn/main.go` 中导入该包，并在 `main` 中按章节顺序调用 `goNN_主题.Demo()`。
3. 在 `docs/guide/` 下新增正文，命名为 `goNN-主题.md`；正文应说明概念、可运行示例、真实输出和练习，并与实际代码保持一致。
4. 在 `docs/guide/index.md` 的章节规划表中，把该章节的「规划中」改成正文链接和配套代码路径。
5. 在 `docs/.vitepress/config.mts` 的导航和侧边栏中加入可点击链接，不能只保留「规划中」文本。
6. 在 `README.md` 的章节进度表中同步章节标题、正文链接和代码目录。
7. 如果引入依赖或构建配置，必须同步修改 `go.mod` 与 `go.sum`；能用标准库解决的不引入第三方库。
8. 为章节里的关键逻辑补 `_test.go` 单元测试；涉及跨包行为时补充 `test/` 下的集成测试。

## 章节正文模板

每章正文按下面的骨架写，顺序固定；缺哪一段要在交付说明里写清原因：

1. **导入段**：承接上一章，说清这一章解决什么问题、为什么重要；结尾一句指向本章配套代码路径和运行命令，例如「本章配套代码在 `internal/chapter/go05_functions/`，执行 `go run ./cmd/go-learn` 可以看到全部输出」。
2. **小节正文**：按 `## 5.1 标题` 顺序编号，一节只讲一个主题，通常 3–8 节。每节的展开顺序是「先给结论 → 最小可运行示例 → 实测输出 → 边界与坑」。
3. **报错与反例**：把本章最容易踩的错误集中成一节，标题形如「N 个真实报错怎么读」，贴真实编译或运行输出，并指出修复线索在哪一行。
4. **常见坑速查**：三列表格「现象 / 原因 / 解法」。
5. **练习与参考答案**：4–6 道题，参考答案用 VitePress 折叠块包起来（`::: details 第 N 题` … `:::`），答案里要说明「为什么这样写更好」。
6. **小结**：用要点列表复述本章结论（通常 5–8 条），最后一句预告下一章。

对应代码：每章一个包，`Demo()` 内按 `--- N. 小节 ---` 分段输出，让正文小节和运行输出能一一对上。

## 体量与详略基准

| 对象 | 量级 | 说明 |
| --- | --- | --- |
| 单章正文 | 约 1.6–3.6 万字节（400–800 行） | 基础章偏短，进阶和工程章偏长 |
| 章节示例包 | 约 150–500 行 | 复杂主题（HTTP、数据库、综合实战）允许在同一个包目录下拆成多个 `.go` 文件 |
| 注释 | 每个导出标识符一行注释，复杂逻辑前一段导向说明 | 不写「定义变量」「调用函数」式旁白 |
| 练习 | 4–6 题，带折叠参考答案 | 至少一题是改写正文示例，而不是重复正文 |

详略原则是**讲透一个主题，而不是罗列全部 API**：标准库章节只讲常用、易错、能对照示例的部分，冷门 API 在速查表里提一句即可。

## 实测输出与报错引用

- 正文里的输出必须来自当前仓库的实际运行结果，不凭记忆写；涉及版本差异、并发时序、性能数字时尤其如此。
- 报错必须贴真实输出（编译器、`go vet`、`-race`、panic 栈），保留文件与行号，并与最小示例保持一致。
- 会随版本或运行变化的内容（panic 头、goroutine 编号、map 遍历顺序、耗时）要在正文里点明「每次不一样」。
- 拿不准的行为先写最小例子跑一遍再下结论；结论和代码冲突时以实际运行结果为准。

## 编码与文件操作

- 所有 Markdown、Go、TypeScript 和 JSON 文件必须使用 UTF-8 编码。
- 读取文件时显式指定 UTF-8（PowerShell 用 `Get-Content -Encoding UTF8`）；写入文件使用 UTF-8 无 BOM，避免默认编码造成乱码。
- 修改文档时优先使用小范围补丁，不要为了改一行而整体转换或重写已有文件。
- 不得把 PowerShell 的 `` `r``、`` `n`` 或 `` `r`n`` 字面量写入 Markdown、配置或源码。
- 修改后检查中文内容、换行和 Markdown 表格列数，确认没有 `�` 等替换字符。

## 验证要求

完成章节后至少执行：

```bash
gofmt -l .
go vet ./...
go test ./...
go run ./cmd/go-learn
```

沙箱或 CI 环境下 `GOCACHE` 不可写时，先把 `GOCACHE` 指到一个可写目录再执行上面的命令；不要把「缓存目录不可写」误判成代码错误。

如果修改了 VitePress 配置或文档导航，还应执行：

```bash
pnpm docs:build
```

构建因环境权限或网络原因失败时，必须在交付说明中明确指出，不得把未验证的结果表述为构建成功。

## 代码风格

- 遵循 `gofmt` / `goimports` 的默认格式；不要手工维护与 `gofmt` 冲突的排版。
- 包名全小写、简短；本项目章节包保留 `goNN_主题` 前缀，目的是让代码目录和文档章节一一对应。
- 导出标识符用 `PascalCase`，未导出标识符用 `camelCase`；缩写词保持大小写一致（`HTTPServer`、`userID`、`parseURL`）。
- 演示函数保持短小、按主题分段输出：章节开头打 `========== goNN_主题: 标题 ==========`，每个小节打 `--- N. 小节 ---`，结束打 `========== 标题演示结束 ==========`。
- 错误处理立即判断 `if err != nil`；返回时用 `fmt.Errorf("...: %w", err)` 补上下文，不吞掉错误、不忽略返回值。
- 只有示例展示 `panic` / `recover` 行为时才使用它，并写清原因；库代码优先返回 `error`。
- 并发示例必须可复现、可测试：需要同步的地方用 channel 或 `sync` 原语，不靠 `time.Sleep` 抢时序。
- 不使用未使用的导入、变量或死代码；公开 API 应有清晰的可见性边界和文档注释。
- 优先使用标准库；不为简单示例引入新的抽象或第三方库。

## 注释与正文风格

- 注释解释「为什么」、边界条件和语言规则，不重复代码表面行为。
- 包注释写成位于 `package` 上方的 `// Package goNN_主题 ...`；导出函数和类型使用以名称开头的注释（`// Demo ...`）；注释语言与现有章节保持一致，当前仓库以中文为主。
- 注释应短而具体，避免空泛的「定义变量」「调用函数」式旁白；复杂算法或不直观的并发、内存约束前可加一段导向说明。
- 正文中的代码、命令和输出必须可由当前仓库复现；涉及版本、性能或运行时时，以实际命令结果为准。
- Markdown 标题层级连续，表格列数一致；章节正文通常包含目标、示例、常见坑、练习和小结。

## 修改范围与协作

- 只修改完成当前任务所必需的文件；不要顺手重排旧章节、升级无关依赖或格式化整个仓库。
- 发现用户已有改动时先读取并在其基础上修改，不得使用 `git reset --hard`、`git checkout --` 等命令覆盖它们。
- 文档导航、章节进度、`go.mod` 和代码入口属于同一变更，必须保持一致；不要只改其中一处。
- 修改配置或文档编码时，先确认原文件编码，再做局部 UTF-8 修改；发现乱码应从可靠的 UTF-8 来源恢复后再补丁更新。
- 交付说明应列出实际修改的文件、验证命令及任何未完成或受环境限制的检查。
- 依赖下载优先使用国内代理（`GOPROXY`）；不要把 `-mod=vendor` 当作下载手段，只有在确认依赖已缓存时才离线构建。
- `AGENTS.md` 与 `CLAUDE.md` 必须同步修改，不允许只更新其中一份。

## 交付说明

每次交付都要列出：实际新增或修改的文件、执行过的验证命令与结果、受环境限制没做的检查（例如还没装 vitepress 就没跑 `pnpm docs:build`）。不得把未验证的结果表述为通过。

## cgo / unsafe 章节规范

- 讲解 cgo 时必须同时说明类型映射（`C.int`、`*C.char`）、字符串与内存的所有权（`C.CString` 配 `C.free`）、`//export` 反向导出、构建约束（`cgo` 构建标签、`CGO_ENABLED`）以及交叉编译限制。
- 每个跨语言指针接口都要写清长度边界、生命周期、所有权与释放责任；不得只给出一个裸的函数签名而省略契约。
- 字符串示例必须区分 Go 字符串、`*C.char` 与 NUL 结尾问题；切片示例必须显式传递指针与长度。
- cgo 边界不得让 Go 的 panic 穿透到 C；应使用错误码或返回值，并说明线程回调与注册/注销规则。
- `unsafe` 示例必须最小化范围，说明 `unsafe.Pointer` 与 `uintptr` 的转换规则、GC 可见性和 `runtime.KeepAlive` 的作用，并为前置条件、空指针和越界路径添加测试或明确验证命令。
- 涉及外部 C 库时，正文应说明目标平台、静态/动态链接差异、构建工具链和发布时的动态库部署要求。
- 条件允许时应至少补充一种反向调用示例（例如 Python `ctypes` 或 C 调用 Go 导出的函数），说明调用方如何加载动态库、声明参数并维持缓冲区生命周期。
